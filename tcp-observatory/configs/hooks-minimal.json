{
  "_comment": "Minimal configuration - only PreToolUse for quick testing. Port 23518 is default.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "curl -s --connect-timeout 0.5 --max-time 1 -X POST -H 'Content-Type: application/json' -d @- 'http://127.0.0.1:23518/hook?event=PreToolUse' || true"
          }
        ]
      }
    ]
  }
}
